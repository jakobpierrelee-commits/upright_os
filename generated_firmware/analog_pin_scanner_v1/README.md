# analog_pin_scanner_v1

Generated by UpRight.os unified scaffold.

## What this gives you
- Stable serial command contract used by the UI (`GET`, `PID`, `MOTION`, `SETPOINT`, `ARM`, `DISARM`, ...).
- Stable STATUS telemetry keys (`mode`, `ang`, `raw`, `gyro`, `out`, `kp`, `ki`, `kd`, `set`, `kv`, `kx`, ...).
- Factory-standard v2 readiness keys for calibration/anti-drift (`gyro_bias`, `accel_level_offset`, `upright_trim`, `vel_meas`, `vel_target`, `outer_loop_enabled`, ...).
- Board pin map and hardware profile in `config_board.h`.

## What you still implement
- Real IMU read/fusion path in `updateTelemetryStub()`.
- Real motor output path in `applyControlStub()`.
- Optional persistence for `SAVECFG`.

## Generated profile
- `profile.json`

## Build command
`arduino-cli compile --fqbn arduino:avr:nano analog_pin_scanner_v1`
